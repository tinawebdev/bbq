<div class="mt-2 mb-2 d-inline-flex">
  <div class="mr-3">
    <%= link_to image_tag(photo.photo.thumb.url, class: "rounded", style: "height: 80px"), photo.photo.url, :data => { :lightbox => "image" } %>
    <p><small>Автор: <%= link_to photo.user.name, user_path(photo.user) %></small></p>
  </div>
</div>
